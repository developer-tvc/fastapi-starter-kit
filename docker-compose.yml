services:
  fast_skeleton:
    container_name: fast_skeleton
    build: .
    ports:
      - "8000:8000"

    env_file:
      - .env

    volumes:
      - .:/app

    restart: always

    networks:
      - app_network


networks:
  app_network:
    driver: bridge